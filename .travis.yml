sudo: false
language: generic

matrix:
  include:
    - os: linux
      dist: bionic
      sudo: false
      addons:
        apt:
          packages:
            - make
            - guile-2.2
            - chezscheme
  fast_finish: true

before_script:
  - script/fetch-configlet
  - bin/configlet lint .

script:
  - make ci
